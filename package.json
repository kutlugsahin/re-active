{
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/store",
    "packages/react",
    "packages/app"
  ],
  "scripts": {
      "demo": "cd ./packages/app&&yarn start",
      "lib": "cd ./packages/re-active && yarn run build",
      "publish:lib": "cd ./packages/re-active && npm publish core",
      "inc": "node build.js inc"
  }
}